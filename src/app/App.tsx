import { FindPlaceForm } from "../features/FindPlaceForm";

import styles from "./App.module.css";

function App() {
  return (
    <div className={`${styles.App}`}>
      <FindPlaceForm />
    </div>
  );
}

export default App;
